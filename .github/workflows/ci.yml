name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality-gates:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Run quality gates
        run: scripts/check.sh
